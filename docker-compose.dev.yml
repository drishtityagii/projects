version: '3.9'
services:
  web:
    build:
      context: ./apps/web
    env_file:
      - ./apps/web/.env.local
    ports:
      - "3000:3000"
    volumes:
      - ./apps/web:/app
      - /app/node_modules